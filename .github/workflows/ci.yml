name: CI/CD Pipeline - Sistema Escolar

on:
  push:
    branches:
      - main
      - master
  pull_request:
    branches:
      - main
      - master

jobs:
  build:
    name: Stage de Build
    runs-on: ubuntu-latest
    steps:
      - name: Baixar código do repositório
        uses: actions/checkout@v4

      - name: Configurar Java 17
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'
          cache: maven

      - name: Compilar projeto
        run: mvn clean compile

  test:
    name: Stage de Test
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Baixar código do repositório
        uses: actions/checkout@v4

      - name: Configurar Java 17
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'
          cache: maven

      - name: Executar testes
        run: mvn test

  deploy:
    name: Stage de Deploy
    runs-on: ubuntu-latest
    needs: test
    steps:
      - name: Simular deploy
        run: echo "Deploy realizado com sucesso!"
=======
        run: echo "Deploy realizado com sucesso!"
>>>>>>> b434ed327e6b9c3b787aed6cfe93847e9536113e
